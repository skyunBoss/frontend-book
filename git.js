// git flow

// git hooks

// git rebase / merge
// 都是用于一个分支获取并且合并到当前分支，但是采取不同的工作方式
// merge 自动创建一个新的commit，如果使用git tools时且commit频繁操作，分支图会特别乱
// rebase 会合并之前的commit历史，如果合并出现问题不容易定位，因为re-write了history
